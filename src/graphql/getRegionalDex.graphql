query getRegionalDex($pokedexName: String!) {
  pokemon_v2_pokemondexnumber(
    where: { pokemon_v2_pokedex: { name: { _eq: $pokedexName } } }
    order_by: { pokedex_number: asc }
  ) {
    pokedex_number
    pokemon_species_id
  }
}
